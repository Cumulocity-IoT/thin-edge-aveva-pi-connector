{
  "name": "pi-historian-flow",
  "version": "1.0.0",
  "description": "tedge-flow that polls AVEVA PI Web API and publishes measurements to Cumulocity",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --platform=neutral --format=esm --outfile=dist/main.js",
    "build:watch": "esbuild src/main.ts --bundle --platform=neutral --format=esm --outfile=dist/main.js --watch",
    "package": "COPYFILE_DISABLE=1 tar czf ../pi-historian-flow.tar.gz flow.toml params.toml.template dist/main.js",
    "deploy:local": "cp -Ra . /etc/tedge/mappers/c8y/flows/pi-historian-flow",
    "undeploy:local": "rm -rf /etc/tedge/mappers/c8y/flows/pi-historian-flow && systemctl restart tedge-mapper-c8y"
  },
  "devDependencies": {
    "esbuild": "*",
    "typescript": "*"
  }
}